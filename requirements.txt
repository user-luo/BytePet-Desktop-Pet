# BytePet 电子宠物 - 依赖
PyQt5==5.15.11          # GUI 框架（透明置顶窗口、托盘、气泡）
pywin32==311            # Windows API（桌面图标、活跃窗口、开机自启、互斥锁）
Pillow==10.4.0          # 图像处理（抠图、缩放、透明合成）
rembg                   # AI 抠图（去除猫图背景，保留细节）
pyinstaller             # 打包成 exe
